name: opto-sync real web application e2e

on:
  pull_request:
    paths:
      - 'src/**'
      - 'public/**'
      - 'tests/**'
      - 'scripts/**'
      - 'astro.config.*'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/opto-sync-real-web.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  real-web:
    uses: opto-sync/opto-sync-e2e/.github/workflows/real-web-app-reusable.yml@a8e4a5cb1fc67ddc1e415d910b18aa99056b2321
    with:
      expect_offline_shell: false
